part1：生成一条用于长期检索的剧情摘要并标注定位锚点，并按 part2 的规则抽取本轮值得长期保留的记忆条目。禁止思考。

摘要正文要求：
- 30~60 字，完整句子，用过去时叙述已发生的事
- 只写本轮实际发生的关键动作、转折和结论，不复述设定、不写气氛铺垫
- 这句话会脱离上下文被单独召回，必须自己读得懂：不要出现"你""我""他""对方""某人"这类需要联系上下文才能理解的代词
- 用户侧称"{{USER_NAME}}"，角色侧称"{{CHARACTER_NAME}}"，其余人物写出名字；只有原文里根本无法判断时才可泛称

锚点要求：
- `scene`：本轮发生的地点或场景，不超过 8 字；无法判断时填空字符串
- `cast`：本轮在场且有实际行为的人物名，最多 4 个；无法判断时填空数组

{{user}}：{{USER_MESSAGE}}

{{char}}：{{ASSISTANT_MESSAGE}}

────────────────────────────────────────
part2：长期记忆

长期记忆不是剧情日志，而是“当前活跃约束索引”。状态记忆另行维护，本模块只记录未来仍生效的因果、条件、联动、秘密、期限、禁忌和任务约束。

默认输出：无。

仅当本轮信息满足“新 + 活跃 + 长期 + 有明确锚点 + 会约束未来 + 不能被状态记忆替代 + 值得占用有限槽位”时，才输出更新。

类型：
规则 / 任务 / 人物 / 关系 / 势力 / 地点 / 物品 / 秘密 / 契约 / 禁忌

保留级别：
P5核心约束：必须遵守；违反会造成世界观、主线、硬规则、死亡/毁灭、身份、契约等重大矛盾。
P4强约束：高度影响人物行动、关系走向、任务成败、阵营冲突或关键物品使用。
P3活跃伏笔：当前仍有效，未来可能影响选择、揭示、风险、误会或冲突。
P2弱参考：可由状态记忆/摘要/上下文替代，不写进长期记忆。
P1废弃：不记录。

长期记忆只允许输出 P3/P4/P5。

时效：
永久 / 持续 / 直到X / X之前 / 未公开前 / 条件解除前 / 任务完成前

内容要求：
- 每行一条。
- 写当前仍生效的约束，不写剧情流水。
- 每轮最多新增0-1行。
- 无明确后续约束则不记录。
- 记录固定格式的前缀：[类型|保留级别|锚点|时效]
- 不记录临时情绪、动作、寒暄、气氛、重复设定、单纯事件、无后续影响的铺垫。
- 没有在本轮剧情中发生改变的世界观，不得记录。

────────────────────────────────────────
输出格式（严格 JSON，不要加任何额外说明、不要 markdown 围栏、不要思考过程）：

```
{"scene":"<地点或场景>","cast":["<人物名>"],"summary":"<摘要正文，30–60字>","memory":["<[类型|保留级别|锚点|时效] 内容>"]}
```
或
```
{"scene":"<地点或场景>","cast":["<人物名>"],"summary":"<摘要正文，30–60字>","memory":[]}
```

- `summary` 必填；不允许空字符串
- `scene` 是字符串，`cast` 是字符串数组，二者可以为空但不能省略
- `memory` 是数组，最多1条，10–20 字一句话，知识条目风格，不加序号、不加分类前缀
- 若没有任何条目满足全部要求，输出 `"memory":[]`，**禁止**编凑
