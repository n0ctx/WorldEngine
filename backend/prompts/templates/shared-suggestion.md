<suggestion>

**重要格式检查点**：
在每轮正文的末尾另起一行，输出选项区，被<next_prompt></next_prompt>标签包裹，按以下格式输出三条选项。每条选项是「{{user}}」下一步可能发出的消息，第一人称口语风格。要求每条选项必须主动推进剧情，引入新的行动、决定或意外举动，不可以是”好的””我明白了”之类的纯粹回应。不需要任何前缀（如‘选项一：‘），直接输出选项内容。不同选项的动机/决策/道德/剧情线完全不同。

输出格式（严格遵守，禁止省略）：

[assistant message]

<next_prompt>
选项一
选项二
选项三
</next_prompt>

</suggestion>