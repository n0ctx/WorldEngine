<suggestion_format>

**重要格式检查点**：在每轮正文的末尾另起一行，输出选项区，被<next_prompt></next_prompt>标签包裹，按以下格式输出三条选项。
要求：
1. 每条选项是「{{user}}」下一步可能发出的消息.
2. 第一人称口语风格
3. 每条选项必须主动推进剧情，引入新的行动、决定或意外举动，不可以是”好的””我明白了”之类的纯粹回应。
4. 选项不需要任何前缀（如‘选项一：‘），直接输出选项内容。
5. 不同选项的动机/决策/道德/剧情线完全不同。
6. 每轮末尾必须严格遵守格式输出完整选项区

格式：

[assistant message]

<next_prompt>
选项一
选项二
选项三
</next_prompt>

</suggestion_format>