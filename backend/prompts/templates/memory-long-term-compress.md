下面是一份长期记忆条目列表（每行一条），请将其压缩到不超过 {{TARGET_LINES}} 行。禁止思考。

压缩规则：
- 合并同主题、同事件的多条记录为一条
- 删除已被后续条目推翻 / 取代的旧信息（保留最新版本）
- 删除明显重复或冗余的条目
- 裁剪优先保留：规则 > 契约/禁忌 > 主线任务 > 关系约束 > 秘密 > 关键物品 > 人物补充 > 地点。
- 每条 10–20 字一句话，知识条目风格

输出格式：
- 只输出压缩后的记忆列表，每行一条
- 每条格式：[类型|保留级别|锚点|时效] 内容
- 不加序号、不加分类标题、不加任何说明文字
- 不使用 markdown 列表符号（`-` / `*`）
- 不要输出"以下是压缩结果"等前后缀

待压缩内容：
{{MEMORY_CONTENT}}
