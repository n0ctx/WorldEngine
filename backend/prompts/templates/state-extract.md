你是角色人设分析助手。请阅读下面的人设资料，为"{{NAME}}"推断在当前世界下各状态字段的建议值。

要求：
1. 逐个字段独立判断：人设资料里有明确信息、或能从上下文合理推断时才给出值；推断不出来的字段禁止编造，直接不要在结果中出现该字段的 key
2. 严格按字段类型输出：
   - list：字符串数组，每条尽量简短（不超过 {{TEXT_MAX_LENGTH}} 字），最多 {{LIST_MAX_ITEMS}} 条
   - number：纯数字（不要带单位或文字）
   - enum：必须是"可选值"里给出的原文之一，不要自创新选项，也不要意译
   - text：简短文本（不超过 {{TEXT_MAX_LENGTH}} 字）
   - boolean：true 或 false
   - datetime：ISO 局部时间字符串 "YYYY-MM-DDTHH:mm"
3. 严格遵守每个字段的说明、更新说明、可选值、取值范围等约束
4. 不要重复人设原文的大段描述，每个字段的值只保留与该字段直接相关的结论
5. 只返回一个 JSON 对象，key 为字段的 field_key，value 为推断出的值；没有把握的字段不要出现在结果里；不要添加任何解释文字，也不要用 markdown 代码块包裹

以下是"{{NAME}}"的人设资料：

{{PERSONA_TEXT}}

以下是需要推断的字段定义（schema）：

{{SCHEMA}}

请只返回 JSON 对象。
