// Slot identifiers for PageLayout. Kept in a separate non-component file so
// HMR / react-refresh boundaries stay clean.
export const PageLayoutSlots = Object.freeze({
  HEADER: 'header',
  LEFT: 'left',
  MAIN: 'main',
  RIGHT: 'right',
  INSPECTOR: 'inspector',
  OVERLAY: 'overlay',
});
