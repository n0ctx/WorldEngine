{
  "name": "worldengine-desktop",
  "version": "0.0.5",
  "private": true,
  "description": "AI-assisted immersive roleplay and creative writing desktop app.",
  "author": "n0ctx",
  "type": "module",
  "main": "src/main.js",
  "scripts": {
    "clean-dist": "node scripts/clean-dist.js",
    "prepare-build": "node scripts/prepare-build.js",
    "start": "electron .",
    "build": "npm run clean-dist && npm run prepare-build && electron-builder && rm -f dist/*.blockmap",
    "dist": "npm run clean-dist && npm run prepare-build && electron-builder --mac --win && rm -f dist/*.blockmap"
  },
  "devDependencies": {
    "electron": "^39.8.9",
    "electron-builder": "^26.0.0",
    "extract-zip": "^2.0.1"
  }
}
