    # WorldEngine — 项目需求文档

> 这是给自己看的需求文档，记录"为什么做"和"做什么"。CLAUDE.md 是给 AI 看的工作手册。
>
> 若本文件与 `CLAUDE.md` / `SCHEMA.md` 冲突，以后两者为准。

---

## 是什么

WorldEngine 是一个本地运行的 Web 端 LLM 前端，专注于**角色扮演和创意写作**场景。

对标 SillyTavern，但做减法和优化：
- 删除使用频率低的功能（TTS、图生图、Visual Novel 模式等）
- 对核心功能深度优化（记忆系统、提示词系统、UI 交互）
- 牺牲部分可配置性，换取更好的开箱即用体验

---

## 核心设计理念

### 层级结构（比 ST 多一层"世界"）

```text
全局 → 世界 → 角色 → 会话
```

- **全局**：文风偏好、API 配置、默认提示词
- **世界**：世界观设定、规则、氛围——一个世界可以有多个角色
- **角色**：人设、背景——属于某个世界
- **会话**：具体的一次对话

**为什么加"世界"层**：ST 里同一个世界观下的多个角色，需要在每个角色卡里重复设定世界背景。WorldEngine 把世界背景提到世界层，角色只管角色自己的事。

### 提示词系统

两类提示词：
1. **System Prompt**：传统方式，全局/世界/角色三层合并注入
2. **Prompt 条目（自动触发）**：每轮注入所有条目的简介，代码根据 embedding 相似度和关键词匹配决定哪些条目展开正文。避免把所有内容塞进上下文，节省 token，保持 LLM 专注

OOC 内容（用户用 (( )) 标注的旁白说明）原文进入上下文，不做特殊过滤，由 AI 自行理解处理。
补充约束：
- OOC 参与当轮对话理解、会话标题生成和 Prompt 条目触发
- OOC 不应沉淀为角色长期状态或世界历史事实

### 记忆系统（核心创新）

**层一 - 原始 Session**：
- 全量保存，不丢失任何信息
- 会话达到轮次阈值时，`maybeCompress` 触发压缩：生成 summary、归档旧消息、更新世界时间线、embedding 向量化；短会话（低于阈值）无 summary
- 召回逻辑：embedding 搜索历史 summary → 渲染摘要注入上下文 → preflight 决定是否展开原文 → 渐进式读取原文（T27/T28）
- 类似"先看目录，再翻正文"

**层二 - 角色状态栏**：
- 每个世界可前端配置一套角色状态字段模板
- 每个角色基于该模板持有自己的当前状态值
- 用于记录角色的持续性个人状态，例如关系、伤势、心情、目标、身份等
- 后端在对话后按配置异步更新；对 LLM 注入时渲染为可读文本

**层二补充 - 玩家状态栏**：
- 每个世界可前端配置一套玩家状态字段模板（与角色状态字段模板独立）
- 每个世界有且仅有一个 persona 实例，持有一份当前值
- 玩家卡只有 name 与 system_prompt 两个基础字段，**不含 Prompt 条目**（与角色不同）
- 用于记录玩家的持续性个人状态，例如体力、背包、当前任务等
- 与角色状态栏同机制：后端在对话后按配置异步更新；对 LLM 注入时渲染为可读文本

**层三 - 世界状态栏**：
- 每个世界可前端配置一套世界状态字段模板
- 每个世界持有一份共享的当前世界状态
- 用于记录世界层的持续性公共状态，例如时间、局势、秩序、天气、地点状态、主线阶段等
- 后端在对话后按配置异步更新；对 LLM 注入时渲染为可读文本

**层四 - 世界时间线**：
- 记录这个世界所有历史会话的摘要档案，每个会话一行（one-row-per-session）
- 压缩触发时自动 upsert；前端记忆面板展示最近 5 条
- 给 AI 提供世界级别的历史背景

---

### 写作空间（M5 新增）

有别于聊天模式的另一种创意写作模式：
- 同一个世界下可以同时激活多个角色参与写作
- 消息流以散文形式展示（无气泡），适合叙述性写作
- 会话类型 `mode='writing'`，通过 `writing_session_characters` 联结表动态管理激活角色
- 与聊天会话共享记忆系统（状态更新、时间线、压缩）

---

### 不做的功能

- 多用户系统
- 图片生成（SD、ComfyUI 等）
- TTS 语音
- Visual Novel 模式
- 多角色群聊（MVP 阶段）
- 云端同步
- 插件市场
- 复杂宏脚本系统
- SillyTavern PNG 角色卡兼容
- SillyTavern Regex 扩展格式兼容（仅实现同类能力，不导入 ST 正则包）
- 深浅色主题切换（外观完全交给用户自定义 CSS）
- 用户自定义 Prompt 组装顺序
- 自动备份（MVP 阶段不做）
- 消息分支 / Swipe（重新生成覆盖旧版本）
- 备选开场白（first_message 仅支持单条）
- Author's Note

---

## 用户体验原则

1. **开箱即用**：新用户创建一个角色就能开始聊，不需要理解复杂配置
2. **渐进披露**：高级功能存在但不强迫用户学习
3. **本地优先**：数据不出本机，API Key 只存本地
4. **简洁 UI**：参考 ST 的交互模式，但视觉上更简洁现代
5. **长期可用**：世界观、角色状态、历史关系可以持续积累

---

## 技术选型理由

| 选择 | 理由 |
|---|---|
| React + Vite | 组件化，适合模块化开发；Vite 构建快 |
| TailwindCSS | 不需要手写 CSS 文件，AI 协作友好 |
| Node.js + Express | 和前端同语言（JS），降低认知负担 |
| SQLite | 单文件数据库，无需安装服务，本地运行完美 |
| JSON 导出 | 数据可读，方便手动备份和迁移 |

---

## 里程碑

| 里程碑 | 目标 | 状态 |
|---|---|---|
| M0 | 项目骨架搭建完成，可以跑起来 | ✅ 完成 |
| M1 | 能和角色对话（流式输出），世界/角色/会话基础功能可用，数据能保存 | ✅ 完成 |
| M2 | 提示词系统可用（三层 System Prompt + Prompt 条目自动触发） | ✅ 完成 |
| M3 | 记忆系统可用（session summary + 角色状态栏 + 世界时间线 + 记忆召回 + 渐进展开） | ✅ 完成 |
| M4 | 完善体验（导入导出、自定义 CSS 片段、正则替换规则、Slash 命令、Continue / Impersonate、上下文压缩） | ✅ 完成 |
| M5 | 体验打磨（写作空间、WYSIWYG 编辑、状态字段编辑重构等） | 🚧 进行中 |
| 公开发布 | 文档、README、清理代码 | ⬜ 未开始 |

> 说明：本文件只定义产品层面的里程碑分组，具体开发顺序、任务拆分和实现细节以 `ROADMAP.md` 为准。

